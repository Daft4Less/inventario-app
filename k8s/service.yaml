apiVersion: v1
kind: Service
metadata:
  name: inventario-app-service
spec:
  type: NodePort
  selector:
    app: inventario-app
    tier: base
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080