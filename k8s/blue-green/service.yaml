apiVersion: v1
kind: Service
metadata:
  name: inventario-app-bluegreen-service
spec:
  type: NodePort
  selector:
    app: inventario-app
    color: blue
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30086